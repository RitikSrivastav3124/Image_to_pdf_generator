# 📸 Image to PDF Generator

A Flutter app to convert images from camera/gallery into a **PDF file** easily and quickly.

Converted from a starter Flutter template, this project lets users select images and generate a PDF document that can be saved and shared.

---

## 🔥 Features

✨ Convert multiple images into a single PDF  
📷 Select images from Camera  
🖼️ Select images from Gallery  
📝 Name and save the generated PDF  
📤 Share the PDF easily  
🗂️ Manages saved PDF files  
🗑️ Delete unwanted PDFs

> *(Add more features if you implement them — e.g., sorting, preview, page reorder — as you build your app)*

---

## 📦 Tech Stack

This project is built using:

- **Flutter** – Cross-platform UI toolkit  
- **Dart** – Programming language  
- PDF generation packages and image pickers (flutter plugin deps in `pubspec.yaml`)

---

## 🚀 Getting Started

Follow these steps to run the project locally:

### 🛠 Prerequisites

Make sure you have Flutter installed:

```bash
flutter --version

